version: '2'

services:
  mongodb:
    volumes:
      - progdash-mongo-data:/data/db
      - progdash-mongo-config:/data/configdb
    ports:
      - 27017:27017

  mongoadmin:
    ports:
      - 8081:8081

volumes:
  progdash-mongo-data:
    driver: local
  progdash-mongo-config:
    driver: local